1.) Create a npm project and install Express.js (Nodemon if you want)
2.) Create an Express.js app which funnels the req through 2 middleware function that log something to the console and reture one res.
3.) Handel req to "/" and "/users" such that each req only has one handler / middleware that does something with it (eg send dummy res)